# Tasks

- [ ] Define `EventEnvelope` and update `EventBus` publish/subscribe surface
- [ ] Update event logging to emit envelope metadata consistently
- [ ] Update server SSE stream to send envelopes (and set SSE id)
- [ ] Update CLI/TUI event parsing to handle envelope payloads
- [ ] Add tests for envelope serialization and SSE event shape
- [ ] Run workspace test suite and fix failures

