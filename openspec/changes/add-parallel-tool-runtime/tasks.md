# Tasks

- [ ] Define tool execution modes and runtime interface
- [ ] Implement `ToolCallRuntime` with selective parallelism and determinism
- [ ] Assign execution modes to existing tools conservatively (default sequential)
- [ ] Update agent tool-call loop to use the runtime
- [ ] Add tests for parallel execution, ordering, and exclusivity
- [ ] Run workspace test suite and fix failures

