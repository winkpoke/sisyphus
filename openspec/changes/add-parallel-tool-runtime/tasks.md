# Tasks

- [x] Define tool execution modes and runtime interface
- [x] Implement `ToolCallRuntime` with selective parallelism and determinism
- [x] Assign execution modes to existing tools conservatively (default sequential)
- [x] Update agent tool-call loop to use the runtime
- [x] Add tests for parallel execution, ordering, and exclusivity
- [x] Run workspace test suite and fix failures
